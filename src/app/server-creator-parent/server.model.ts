export class Server {
    name: string;
    content: string;
    type: string;

    constructor(name, content, type) {
        this.name = name,
        this.content = content,
        this.type = type
    }
}